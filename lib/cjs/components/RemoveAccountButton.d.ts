import React from 'react';
interface RemoveAccountButtonProps {
    confirmationText?: string;
    agreeText?: string;
    className: any;
}
export declare const RemoveAccountButton: ({ confirmationText, agreeText, className, ...restProps }: RemoveAccountButtonProps) => React.JSX.Element;
export {};
